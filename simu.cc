#include "simu.hh"

/*Simu& Simu::operator+=(Object* rhs){
  vect_objet.push_back(rhs);
  return *this;
}

Simu operator+(Simu lhs,Object* rhs)
{
  lhs += rhs;
  return lhs;
}

void Simu::update(){

  for(size_t i=0;i<vect_objet.size();i++){
    vect_objet[i]->update(s);
    vect_objet[i]->draw(s);
  }
  s.flip();

}*/
